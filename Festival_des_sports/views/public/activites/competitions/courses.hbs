<head>
    <title> Liste courses </title>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.6.1/font/bootstrap-icons.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"
            integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p"
            crossorigin="anonymous"></script>
</head>

<style>
    .card {
        flex-direction: row;
    }
    .card img {
        width: 30%;
        height:auto;
    }
    .card-text {
        font-size: 15px;
    }
    .card-body {
        flex: 1 1 auto;
        padding: 1rem 2rem;
    }
    p {
        margin-top: 0;
        margin-bottom: 3px;
    }
    .btn{
        margin-top: 10px;
    }
</style>
<div class="container">
    <div class="row paragraphe">
        <br><br>
        <div class="col"><h1>Courses</h1>
            <span>
            Natation, athlétisme, VTT et bien d’autres encore seront au rendez-vous pour vous faire passer un momentinoubliable entre amis, en famille ou en solitaire!
            </span>
        </div>
    </div>

    <div class="row ligne"></div>

    <div class="row">
        <div class="col my-auto">
            <div class="d-flex justify-content-center">
                <div class="form-inline">
                    <input class="form-control" id="search" type="search" placeholder="Rechercher" aria-label="Search">
                    <button class="btn my-2 my-sm-0" id="deselect"><i class="bi bi-x-square-fill"></i></button>
                </div>
            </div>
        </div>
        {{# if filtres}}
            <div class="col my-auto">
                <div class="d-flex justify-content-center">
                    <div class="form-inline">
                        {{# each filtres}}
                            <div class="col">
                                <div class="dropdown">
                                    <button type="button" class="btn dropdown-toggle" data-toggle="dropdown" style="background-color: #6ec8cb; font-weight: bold; color: white;">
                                        {{@key}}
                                    </button>
                                    <div class="dropdown-menu">
                                        {{# each this}}
                                            <div class="dropdown-item">
                                                <a><input type="radio" name="option" value="{{this}}">{{this}}</a>
                                            </div>
                                        {{/each}}
                                    </div>
                                </div>
                            </div>
                        {{/each}}
                    </div>
                </div>
            </div>
        {{/if}}
    </div>

    <div class="row mx-auto" id="research">
        {{# if courses}}
            {{# each courses}}
                <div class="col-md-6 py-4 ps-5 my-auto mx-auto">
                    <div class="card shadow-sm">
                        {{# each this.[2]}}
                            <img src="{{this}}" class="card-img-top" alt="...">
                        {{/each}}
                        <div class="card-body">
                            <h3 class="card-title">{{this.[0]}}</h3>
                            {{# each this.[1]}}
                                <p class="card-text"> <b> {{@key}}  : </b> {{this}}</p>
                            {{/each}}
                            <a href="#" class="btn btn-primary">Inscription</a>
                        </div>
                    </div>
                </div>
            {{/each}}
        {{/if}}
    </div>
</div>
<br><br>

<script>
    $(document).ready(function(){
        $("#search").on("keyup", function() {
            $('input[name="option"]').prop('checked',false);
            var value = $(this).val().toLowerCase();
            $("#research .col-md-6").filter(function() {
                $(this).toggle($(this).text().toLowerCase().indexOf(value) > -1)
            });
        });
    });

    $(document).ready(function(){
        $('input[name="option"]').on("click", function() {
            var value = $(this).val().toLowerCase();
            $("#research .col-md-6").filter(function() {
                $(this).toggle($(this).text().toLowerCase().indexOf(value) > -1)
            });
        });
    });

    $(document).ready(function(){
        $('button[id="deselect"]').on("click", function() {
            $('input[name="option"]').prop('checked',false);
            var value = $(this).val().toLowerCase();
            $("#research .col-md-6").filter(function() {
                $(this).toggle($(this).text().toLowerCase().indexOf(value) > -1)
            });
        });
    });
</script>